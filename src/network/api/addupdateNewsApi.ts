import NetworkCall from "../networkCall";
import {message} from "antd";
import FooterRequest from "../request/footerRequests"

export const addNewsApi = async (body) =>{
    try {
        
    } catch (error) {
        
    message.error(error.message);
    }
}