import { labels } from "./label";
import { placeholders } from "./placeholder";
import { patterns, validations } from "./validation";

const messages = { labels, patterns, placeholders, validations };

export default messages;
