import { labels } from "./label";
import { placeholders } from "./placeholder";
import {
  patterns,
  validations,
  phoneNumberRule,
  mobileCarrierRule,
  firstNameRule,
  lastNameRule,
  middleNameRule,
  emailRule,
  dobRule,
  otpRule,
  phoneRule,
  getCaptchaRule,
  emRule,
  passwordRule,
  confirmPasswordRule,
  usernameRule,
  userPassRule,
} from "./validation";

const messages = {
  labels,
  patterns,
  placeholders,
  validations,
  phoneNumberRule,
  mobileCarrierRule,
  firstNameRule,
  lastNameRule,
  middleNameRule,
  emailRule,
  dobRule,
  otpRule,
  phoneRule,
  getCaptchaRule,
  emRule,
  passwordRule,
  confirmPasswordRule,
  usernameRule,
  userPassRule,
};

export default messages;
